namespace trifenix.connect.agro.index_model.props
{
    /// <summary>
    /// Determina el estado genérico
    /// </summary>
    public enum BoolRelated
    {
        CURRENT = 0,

        GENERIC_DEFAULT = 1,

        GENERIC_ACTIVE = 2
    }
}
